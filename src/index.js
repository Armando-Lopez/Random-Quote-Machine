import React from 'react';
import ReactDOM from 'react-dom';
import QuoteGenerador from './pages/QuoteGenerador';
import './scss/style.css';

ReactDOM.render(
  <React.StrictMode>
    <QuoteGenerador />
  </React.StrictMode>,
  document.getElementById('root')
);